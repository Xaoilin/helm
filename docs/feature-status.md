# Feature Status

Use this matrix when updating documentation or UI copy. `real` means the hosted website has an implemented path; `paused` means provider work is intentionally disabled; `unavailable` means there is no supported connection; `contract-only` means a reviewed implementation boundary exists but no connector is live; `placeholder/simulated` means the integration is intentionally not presented as live; `required-gap` means policy requires the capability but the implemented path is not yet available.

| Area | Status | Notes |
| --- | --- | --- |
| Hosted GitHub Pages website | `real` | The browser website is the only supported Sabah One product runtime. Release detection runs before account loading, with existing pending-write, editor, draft, and hidden-tab reload guards. Failed initial account loads expose an explicit Retry connection and truthful bounded-retry guidance. Protected promotion, Pages and Supabase deployment, and handoff verification require the current candidate's tested tree and latest successful evidence; missing or stale receipts fail closed. |
| Supabase account persistence | `real` | Authenticated RLS reads, semantic mutations, idempotent receipts, tombstones, explicit ordering, private account Broadcast, and version-gap recovery make Supabase authoritative. KAN-313 separates Realtime notifications from HTTPS readiness, with independent bounded channel retry and version reconciliation. KAN-316 makes the visible/online version safety check run every ten minutes; Broadcast and foreground/reconnect reconciliation remain immediate and coalesced, with no full snapshot reload for an unchanged version. KAN-317 limits each database/channel recovery cycle to five jittered retries, pauses automatic database triggers after failure, and resumes on foreground, online or explicit retry. Recovery probes before snapshot loading; failed channels are removed to stop SDK retries. Supabase Auth retains its built-in single-flight/backoff behavior, with late bootstrap results unable to replace newer auth events. Transient failures retain confirmed same-account data and drafts with freshness notices; invalid account authorization and account changes fail closed. No offline writes or persistent account-data cache. |
| Page-scoped account loading | `real` | KAN-318 loads shared reminder, reward, Calendar-sync and timer dependencies plus the active page through an atomic scoped read. Deferred providers remain unloaded; same-account confirmed memory has ten-minute per-dataset freshness, and fresh revisits reuse it. Activity actions load in 50-record pages with explicit demand for more. KAN-319 keeps current page and open Lina demand separate from visited cache: private notifications and missed-event metadata refresh only changed needed collections, invalidate inactive cache, and reconcile missed Vault metadata. Local confirmed writes remain immediate; duplicate/out-of-order events cannot hide changed scopes. Voice reactivates cached context and waits for its provider render on every open or wake. Invalid authorization/account changes clear memory; existing bounded recovery remains the only automatic retry owner. See `page-loading.md`. |
| Encrypted Secrets vault | `real` | The signed-in Secrets surface stores searchable account metadata and encrypted values through Supabase Vault RPCs. Values are masked, revealed one at a time, cleared from browser UI state when hidden or the session changes, and excluded from records, Broadcast, logs, exports, and assistant context. Archive and Restore are reversible; bulk export, sharing, autofill, permanent deletion, and assistant access are absent. |
| Google Calendar OAuth | `real` | Multi-account Calendar uses hosted authorization-code exchange and server-held refresh credentials. Reconnect is explicit and account-scoped. |
| Supported integration setup | `real` | Empty and partial account collections retain Google Calendar and GitHub setup. Initial and remote hydration add only missing providers; stored IDs, configuration and historical rows remain intact. The display shows one supported card per provider and never saves its filtered projection. Missing Google deployment configuration points to the site operator, not a nonexistent Settings field. |
| Google Calendar sync | `real` | Passive account-bound sync uses Google as provider source of truth, preserves cache on unsafe partial failures, and never opens consent during ordinary navigation or refresh. |
| Google Calendar diagnostics | `real` | Debug shows hosted readiness, credential health, expiry and failure metadata, migration state, ownership checks, sync counts, blocked reasons, and redacted request diagnostics. |
| Manual calendar accounts | `real` | Manual calendar records belong to the signed-in Sabah One account database and are unavailable while the session is offline. |
| Account app time zone | `real` | Settings supports an optional validated IANA zone with `Automatic` browser fallback. Generic app, assistant, and Calendar time use one resolver; prayer calculations remain bound to the timetable zone and mismatches are labelled. |
| Chat assistant with hosted OpenAI | `paused` | Hosted AI is intentionally paused by default. Authenticated health reports the server mode; chat and voice planner calls return explicit `hosted_ai_paused` responses before any OpenAI request. Chat, Settings and Debug show the pause. Paid chat/voice and model-quality acceptance are deferred, not passed. When explicitly re-enabled, the `assistant-openai` Edge Function verifies a current signed-in user before hosted GPT-5.4-family planning and narration. Public application keys cannot invoke AI; expired sessions prompt sign-in. The browser receives structured turns; Lina validates and executes grounded capabilities, or gives an in-app fallback without guessing when the hosted planner is unavailable. |
| Hosted project billing | `paused` | Authorized operators also receive the explicit paused response without an OpenAI request. The billing function verifies the current user and requires server-owned `assistant_billing_operator` metadata. Billing remains unavailable to ordinary accounts; public keys and benchmark grants are denied. |
| Chat assistant with Ollama | `real` | Settings can select a configured Ollama HTTP endpoint, and Auto may use it when hosted planning is unavailable, but never as a silent fallback from an intentional hosted pause. The same grounding, confirmation, deterministic execution, and verified-narration rules apply. |
| Chat conversation export | `real` | Chat can copy the active conversation or export it as Markdown. |
| Recoverable core interactions | `real` | Typed and quick Chat sends share a synchronous guard, retained draft/error and explicit retry. Secret, Trip wizard and Task editors confirm dirty dismissal and retain existing save semantics; the shared dialog hook manages focus and Escape. Calendar and Knowledge open actions are native buttons with separate actions; More contains and returns focus. Health removal requires confirmation. Enabled-AI recovery is verified with synthetic fixtures while hosted AI stays paused. |
| Operational diagnostics | `real` | Debug → Operations shows a bounded redacted session timeline, disconnect/read/write counters and correlated recovery timings, with JSON export. An independent bounded queue sends normalized metadata to the authenticated first-party collector and retained Supabase operational logs. Account changes clear local diagnostics; collection failure never gates product operations. Scheduled public synthetic probes publish actionable GitHub status and retained receipts without a live user session. See [availability operations](availability-operations.md) for retention limits, controlled faults, infrastructure evidence and recovery objectives. KAN-321 adds a matched workload receipt and protected deployed-frontend fixture proof; see [workload verification](sync-workload-verification.md) for the evidence boundaries. |
| Lina activity log and undo | `real` | Chat and voice mutations create account-backed audit entries with grounded undo metadata. One workflow-named coordinator owns the supported task, prayer, calendar, finance, and knowledge inverse operations. |
| Global Inventory | `real` | Account-backed Owned and Needed records support search, filters, dimensions, specifications, stock changes, acquisition, archive, and reviewed multiline import. Projects link to the same catalogue by stable key. |
| Inventory planning boundary | `real` | The authenticated `sabah-one-inventory-mcp` Edge Function exposes seven narrow Inventory tools through Supabase OAuth, RLS, and dedicated RPCs. It does not expose Secrets, finance, calendars, chats, settings, or broad mutations. |
| Employment application tracker | `real` | The account-backed Employment tab opens with a compact active-application overview grouped by company, coloured stage labels, next steps, and a visible last-updated date sourced from recorded recruiting activity. All includes closed applications; search and optional filters narrow the list. Each role expands its full details, compensation, remote evidence, notes, editor, and contact history directly beneath that application. The same account-owned application IDs, semantic mutations, and three confirmed seed records are retained; import timestamps do not masquerade as recruiting dates. |
| Employment external agent access | `real` | Six semantic application/history tools use a separate Employment OAuth approval and account-scoped RPCs. Browser and agent edits share semantic mutations; retries are idempotent and revocation blocks subsequent access. Scheduled Codex agents reconcile recruiting sources after their Employment connection is authorized. |
| Cross-feature external agent access | `required-gap` | `docs/agent-access.md` makes domain-scoped MCP the required external AI interface. Inventory and Employment have separate external domains; other features must add a narrow MCP contract when materially changed. Secrets remain intentionally excluded. |
| AI diagnostics in Debug | `real` | Debug shows hosted assistant readiness, the latest planning and validation trace, pending confirmation, execution facts, narration response, and hosted usage estimates without exposing tokens. |
| User-facing Database / AI Health Center | `real` | Dashboard and Settings show Supabase session, Broadcast, Google Calendar, hosted assistant, and voice readiness with actionable reconnect or unavailable states. |
| Night Compass prayer-first dashboard | `real` | Dashboard renders the canonical five-prayer sequence first, followed by one of 206 curated Quran passages, Learn and Move progression, and one compact Tasks route. The reading card shows full Tanzil Arabic and Pickthall English side by side at every screen width, with a reference, translator credit and source links. The shuffled 206-day cycle uses every passage before repeating and stays consistent across refreshes and devices; 133 passages concern prayer, remembrance or dua. Prayer, Learn and Move achievements use a compact top-right receipt, inset from device safe areas, with no screen-wide glow or input blocking; phones keep the same compact layout at the top. See `quran-motivation-review.md` for the selection review and Arabic/English text-integrity receipts. |
| Life Hero concept approval artifact | `prototype/concept` | The approved standalone responsive proof at `concepts/life-hero/index.html` renders the maximum-quality 332,478-vertex / 597,811-triangle native same-body glTF 2.0 rig with one deduplicated 8K texture, opaque natural PBR, a separately toggleable 105,602-vertex graphite concept jacket sharing the same 24-joint skin, and four embedded native clips. The retained 31K-triangle rig is selected only on constrained devices. Native source geometry, skin, inverse binds, and animations remain unchanged; the page remains an approval artifact even though KAN-260 now consumes the pinned assets through a production presenter contract. |
| Life Hero progression and Sabah One evidence | `real` | Supabase owns the versioned seven-stat rules, append-only evidence, immutable award ledger, source and momentum snapshots, deterministic profiles, owner-only RLS, idempotent atomic award and reconciliation RPCs, computed non-punitive conditions, and provenance-only legacy capture. Existing Prayer, Learn, Move, non-prayer task/goal, budget, savings-goal, saving-transfer, avoidable-spend, and Monzo-backed finance behavior maps through documented stable identities and reasons. Raw wealth and product usage cannot award XP. |
| Life Hero dashboard companion | `real / default-off` | An account-owned Settings toggle explicitly opts into the character-based companion; missing and false values leave it unmounted, so existing and new accounts use the non-character dashboard by default. When enabled, the signed-in Night Compass dashboard reads the permanent seven-stat snapshot into a collapsible bottom-right desktop companion and dedicated mobile panel. |
| Life Hero motivational voice | `real` | The companion derives short supportive first-step, renewal, momentum, and steady lines from its current snapshot. Playback is user-triggered only through the existing ElevenLabs/browser-speech output, with visible preparing, speaking, cooldown, stopped, and actionable failure states. Mute cancels audio, text stays visible, repeated actions are rate-limited, and the feature never autoplays, records, adds music, impersonates a character, or mutates progress. |
| Life Hero daily adventure | `real` | The signed-in companion offers one deterministic turn-based encounter whose capability comes from the current Life Hero snapshot. Completion, defeat, and retry never award or remove permanent XP, levels, stats, badges, or trophies. |
| Private product usage analytics | `real` | Signed-in session, navigation, action, outcome, error, and performance events use a typed content-free taxonomy, bounded non-blocking browser batching, database idempotency, owner-only RLS, and a security-definer ingest RPC. The private Activity viewer adds owner-only filters, trends, funnels, coded errors, cold-start handling, and evidence-labelled recommendations with confidence and suggested action; it never acts automatically. Raw domain values, assistant content, credentials, and provider payloads remain rejected, and usage has no Life Hero XP path. |
| Life Hero external evidence routes | `real` | KAN-264 implements the hosted, read-only GitHub App route with selected installation repositories, Vault-held expiring credentials, atomic authored-merged-PR evidence, idempotency, and visible degraded states. Barclays statement and Eight Sleep data-export remain contract-only; Apple Health and Elif B rows record their implemented fallbacks. Eight Sleep's export schema/API and Elif B's provider identity remain explicit unknowns. |
| Apple Health movement evidence | `real` | The signed-in Health surface accepts an explicit Apple Health XML export without requiring a watch or native runtime. It derives only iPhone walking/running step totals per local date, records source/date freshness and stable duplicate identities, submits through the existing owner-scoped Life Hero evidence RPC, and discards raw XML plus unrelated health and location details. Oversized, malformed, unsupported, empty, duplicate, stale, and service-failure states remain visible; the UI states truthfully that browser HealthKit access is unavailable. |
| Elif B learning evidence | `real` | The signed-in Knowledge surface accepts a minimal manual Elif B session confirmation while the provider identity and API remain unverified. Evidence is explicitly self-reported, retains only bounded session metadata and stable duplicate identity, rejects link/contact-like labels and protected content, and submits through the existing owner-scoped Life Hero evidence RPC. The UI never claims provider verification or uses credentialed scraping, hidden computer control, or copied course material. |
| Daily Learn and Move progression | `real` | Signed-in accounts have versioned templates, five cumulative levels, local-date progress logs, editable prayer-relative anchors, account-owned preferences, one snooze, and visible in-app states for unavailable schedules. Progress buttons default to adding the remaining amount for the displayed level target. Each section has an independent, view-local “Add individual steps” switch for one page, minute, or round at a time; reopening the dashboard restores goal completion mode. Existing account progress and five-level rules are unchanged. Each newly crossed L1–L5 threshold receives a restrained visual receipt; ordinary increments stay quiet and reduced-motion keeps the same meaning. |
| Dashboard focus ranking diagnostics | `retired` | KAN-308 removes the unused provider, recommendation requests, timers, ranking and Debug trace. Night Compass retains its domain-driven behavior. Startup clears only the two retired browser caches; historical feedback remains compatible. See `audits/2026-09-07.md`. |
| Voice assistant | `real` | Voice shares the assistant runtime with Chat; hosted AI planning is paused with the same explicit unavailable response. Browser microphone, transcript, wake-word, speech output, and in-app controls are capability-dependent; Chat remains available when a voice capability is unavailable. |
| Wake word | `real` | OpenWakeWord runs in the browser through WASM where supported, using the explicit Lina model plus three shared models. The unused package-default Jarvis asset is retired. |
| Deepgram speech-to-text | `contract-only` | No secure server transport is supplied. Raw browser keys are no longer accepted or used. |
| Browser speech fallback | `real` | Recognition and speech output work where supported. Browser speech is the usable output while hosted AI is paused or secure synthesis is unavailable. |
| ElevenLabs speech output | `paused` | Chat/Voice and Life Hero share authenticated `assistant-speech`; the current hosted AI pause returns before Vault/provider work. When explicitly enabled, the server resolves the selected account-owned Secrets entry and returns audio only. Settings stores a device-only UUID reference; public voice IDs remain shared. Published-key revocation is a separate required security acceptance step; see `voice-provider-security.md`. |
| Browser speech output | `real` | Browser speech synthesis is used where supported when hosted audio is unavailable. |
| Prayer times, outcomes, deadlines, and reminders | `real` | `PrayerProvider` owns validated Jafari times, account-backed outcomes, page-open reminder planning, and diagnostics. Web Notifications are attempted only after explicit permission; the in-app banner is the fallback. Reminders are observed only while the page remains open. |
| Spring Prayer service migration probe | `contract-only` | The Dashboard can check optional Spring Boot liveness and database-health endpoints. The database probe runs `SELECT 1` with server-only Supabase PostgreSQL credentials; the browser sends no user data or database credentials. This slice moves no prayer behavior or data; the existing Prayer path remains authoritative until later migration steps are implemented and accepted. |
| Spring Calendar service migration probe | `contract-only` | The Dashboard can check optional Spring Boot Calendar liveness and database-health endpoints. The service is an empty connectivity skeleton; it does not call Google Calendar or move OAuth credentials, calendar data, or existing provider behavior. |
| Clock surface | `real` | Timers and stopwatches persist as independently mutable account records with custom cards, laps, alarms, and acknowledgement-required alerts. |
| Finance accounts, budgets, and savings goals | `real` | Account-backed records are isolated per signed-in user. |
| Finance Stocks and Options | `real` | Separate dated holdings, plans, conditional vesting/deadlines and option scenarios, with private semantic Equity MCP persistence. Protected deployment and authenticated personal-record import/readback passed at v0.2.159; see `finance-equity.md`. |
| Finance banking review and loans | `real` | Private dated monthly cash flow, current and conditional target budget calculations, high-value spending groups and lender records through a separate Finance MCP. Protected deployment, Finance-only OAuth, authenticated personal review import/readback and browser-reload persistence passed at v0.2.161; see `finance-banking-review.md` for calculation and acceptance boundaries. |
| Health reflection log | `real` | Account-backed quick-entry and recent-history views are implemented. |
| Monzo import | `contract-only` | Browser token configuration is removed. Sync is unavailable until a secure server connection is supplied; manual finance controls remain available. |
| Knowledge base and lifestyle tracker | `real` | Account-backed CRUD converges across signed-in sessions, and Knowledge entries can move between topics without recreation. |
| Projects reference hub | `real` | Searchable Pinned, Projects, and Archived sections expose links, repositories, documentation, display-only prerequisites, and portable guidance. Shared catalogue metadata is keyed by stable `catalogKey`. |
| Trips planner | `real` | Account-backed multi-country trips, ordered legs, itinerary items, bookings, budgets, and explicit calendar import are implemented. |
| GitHub integration | `real` | Hosted GitHub App authorization with selected-repository read-only access. Expiring credentials remain in Supabase Vault; only authored merged pull requests can create fixed Craft evidence, and provider failures preserve existing progress. |
| Slack integration | `unavailable` | Informational notice only; no simulated connect, ready or disconnect actions. Historical account records remain compatible. |
| Linear integration | `unavailable` | Informational notice only; no simulated connect, ready or disconnect actions. Historical account records remain compatible. |
